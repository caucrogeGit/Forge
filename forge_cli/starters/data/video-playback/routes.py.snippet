# forge-starter:video-playback:start
# Branche la route de lecture officielle de Forge Vidéo (streaming HTTP Range) :
#   GET /videos/{uuid}
# Le code métier vit dans le paquet forge-mvc-video ; on ne fait que le brancher.
from forge_mvc_video import register_video_routes

register_video_routes(router)
# forge-starter:video-playback:end
