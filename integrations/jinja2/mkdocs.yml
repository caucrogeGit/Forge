# Documentation embarquée du paquet integrations/jinja2 (ADR-043).
# Agrégée dans le site unique forgemvc.com par le plugin mkdocs-monorepo.
# Le site_name sert de préfixe d'URL : les pages sont montées sous /integrations-jinja2/.
site_name: integrations-jinja2
docs_dir: docs
nav:
  - Moteur de rendu Jinja2 (renderer.py): renderer.md
