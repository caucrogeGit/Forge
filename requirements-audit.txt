# Surface de dépendances tierces RÉELLEMENT EXPÉDIÉES, auditée par pip-audit en CI.
#
# Le cœur (requirements.txt) ne couvre que ses 4 dépendances runtime ; les opt-ins
# apportent des dépendances à surface CVE (Pillow, cryptography, mariadb, ...) qui
# n'étaient jamais scannées. Ce fichier agrège le cœur et les dépendances tierces
# des opt-ins runtime, sans les outils de développement (pytest, mkdocs, ...).
#
# Un garde-fou (tests/meta/test_pip_audit_optin_coverage_001.py) vérifie que chaque
# dépendance tierce déclarée par un paquet opt-in figure ici : à l'ajout d'une
# nouvelle dépendance opt-in, compléter cette liste.

-r requirements.txt

# Opt-ins (dépendances tierces déclarées dans packages/*/pyproject.toml).
# jinja2 est déjà couvert par le cœur (requirements.txt). jsonschema a quitté le
# cœur avec le moteur d'entités (CORE-DROP-JSONSCHEMA-001) : il est désormais listé
# ici, apporté par forge-mvc-entities et forge-mvc-rbac.

# cryptography est la SEULE dépendance sans borne haute, et c'est délibéré
# (DEPS-CRYPTOGRAPHY-NO-CEILING-001). Le motif complet est dans
# tests/meta/test_security_cryptography_mfa_001.py ; en deux phrases : la
# bibliothèque livre ses correctifs de sécurité dans une nouvelle majeure, donc
# un plafond interdit le correctif au moment même où il paraît, et
# forge-mvc-mfa est une bibliothèque, dont le plafond contamine la résolution
# de toute application qui en dépend.
cryptography>=50.0.0
faker>=30,<40
jsonschema>=4.18,<5
mariadb>=1.1.14,<1.2
markupsafe>=2.0,<4
paho-mqtt>=2.1,<3
Pillow>=12.3,<13
psycopg[binary,pool]>=3.1,<4
pyodbc>=5.0,<6
pyotp>=2.9,<3
segno>=1.6,<2
