-r requirements.txt
pytest>=8.0
# Exécution parallèle de la suite (`pytest -n 4`), et surtout `--dist loadfile`,
# posé dans `addopts` : plusieurs fichiers d'intégration sont des scénarios que
# la répartition par défaut éparpille entre workers
# (`TEST-DB-WORKER-ISOLATION-001`). Le greffon était installé sur le poste de
# développement sans être déclaré ici : la CI, qui suit ce fichier, ne le
# connaissait pas et refusait le drapeau.
pytest-xdist>=3.0
build>=1.0
setuptools>=77.0.3
twine>=5.0
mkdocs>=1.6
mkdocs-material>=9.7
# Importé DIRECTEMENT par tests/meta/test_doc_absolute_links_001.py, qui
# vérifie les ancres avec la fonction de slug de MkDocs plutôt qu'avec une
# réimplémentation approximative. Il arrivait déjà par mkdocs ; une
# dépendance transitive suffit tant qu'elle dure, et cesse sans prévenir.
Markdown>=3.6
pymdown-extensions>=10.0
mkdocs-monorepo-plugin>=1.1
mkdocs-glightbox>=0.5
pip-audit>=2.0
ruff>=0.4
pyright>=1.1.400

# Modules opt-in Forge, installés en mode éditable depuis le monorepo.
# Sans ces lignes, les tests qui importent forge_mvc_mfa, forge_mvc_rbac,
# forge_mvc_workflow, forge_mvc_stats, forge_mvc_files, forge_mvc_images,
# forge_mvc_audio, forge_mvc_iot, forge_mvc_video, forge_mvc_entities et
# forge_mvc_mail sont silencieusement skippés (pytest.importorskip), donc
# leur suite ne s'exécute pas en CI. L'extra forge-mvc[all] ne couvre que
# rbac/workflow/stats (tous les autres opt-ins sont exclus, voir pyproject.toml) :
# pour le dev on pointe tous les opt-ins vers le monorepo local afin
# d'exécuter la suite complète.
# Backends BDD (ADR-054) : le cœur est agnostique, il faut un backend installé
# pour que la couche base de données fonctionne (entry point forge_mvc.db_backend).
# Le monorepo installe les deux pour les tester ; le backend par défaut de la
# suite est fixé via DB_BACKEND=mariadb (voir conftest.py).
-e ./packages/forge-mvc-mariadb
-e ./packages/forge-mvc-sqlite
-e ./packages/forge-mvc-postgres
-e ./packages/forge-mvc-mssql
-e ./packages/forge-mvc-admin
-e ./packages/forge-mvc-mfa
-e ./packages/forge-mvc-rbac
-e ./packages/forge-mvc-workflow
-e ./packages/forge-mvc-stats
-e ./packages/forge-mvc-images
-e ./packages/forge-mvc-files
-e ./packages/forge-mvc-iot
-e ./packages/forge-mvc-video
-e ./packages/forge-mvc-audio
-e ./packages/forge-mvc-entities
-e ./packages/forge-mvc-mail
-e ./packages/forge-mvc-i18n
-e ./packages/forge-mvc-qrcode
-e ./packages/forge-mvc-sessions-db
-e ./packages/forge-mvc-settings
-e ./packages/forge-mvc-import-export
-e ./packages/forge-mvc-audit
-e ./packages/forge-mvc-jobs
-e ./packages/forge-mvc-notifications
-e ./packages/forge-mvc-deploy
-e ./packages/forge-mvc-fixtures

# Infrastructure de test partagée (ADR-041) : FakeRequest + plugin pytest
# (fixtures autouse de configuration du noyau, nettoyage, fake_request). Dev only.
-e ./packages/forge-mvc-testing
bandit>=1.7
