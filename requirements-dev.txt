-r requirements.txt
pytest>=8.0
build>=1.0
setuptools>=77.0.3
twine>=5.0
mkdocs>=1.6
mkdocs-material>=9.7
pymdown-extensions>=10.0
pip-audit>=2.0
ruff>=0.4

# Modules opt-in Forge — installés en mode éditable depuis le monorepo.
# Sans ces lignes, pytest plante en collecte sur ~46 fichiers de tests
# qui importent forge_mvc_mfa, forge_mvc_rbac, forge_mvc_workflow,
# forge_mvc_stats. Les packages opt-in ne sont pas (encore) publiés sur
# PyPI, donc l'extra forge-mvc[all] ne peut pas les résoudre — on les
# pointe explicitement vers le monorepo local.
-e ./packages/forge-mvc-mfa
-e ./packages/forge-mvc-rbac
-e ./packages/forge-mvc-workflow
-e ./packages/forge-mvc-stats
