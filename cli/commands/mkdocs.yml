# Documentation embarquée du sous-paquet cli/commands (ADR-043).
# Agrégée dans le site unique forgemvc.com par le plugin mkdocs-monorepo.
# Le site_name sert de préfixe d'URL : les pages sont montées sous /cli-commands/.
site_name: cli-commands
docs_dir: docs
nav:
  - Référence CLI commands (par module):
      - Dispatch des commandes opt-in (optin_dispatch.py): optin_dispatch.md
