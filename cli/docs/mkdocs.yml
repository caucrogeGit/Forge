# Documentation embarquée du sous-paquet cli/docs (ADR-043).
# Agrégée dans le site unique forgemvc.com par le plugin mkdocs-monorepo.
# Le site_name sert de préfixe d'URL : les pages sont montées sous /cli-docs/.
site_name: cli-docs
docs_dir: docs
nav:
  - Référence CLI documentation (par module):
      - Commande docs:pdf (quarkdown.py): quarkdown.md
